### 概念： 顺序 并行 并发

* 顺序： 表示多个操作依次处理，例如：把十个操作交给一个人处理时，这个人要一个一个地按顺序来处理。
* 并行： 表示用于多个操作同时处理，例如：把十个操作交给两个人处理，这两个人就会并行处理。
* 并发： 用于表示 将一个操作分割成多个部分来处理并且允许无序处理。例如，将十个操作分成相对独立的两类，这样便能够开始并发操作了。如果是一个人来处理，那么就是顺序处理分开的并发操作，如果是两个人，那么这两个人就可以并行处理同一个操作了。(单个cpu和多个cpu)(分并发处理的顺序执行与并发处理的并行执行)
